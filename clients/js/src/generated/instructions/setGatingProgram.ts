/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { TOKEN_ACL_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_GATING_PROGRAM_DISCRIMINATOR = 2;

export function getSetGatingProgramDiscriminatorBytes() {
  return getU8Encoder().encode(SET_GATING_PROGRAM_DISCRIMINATOR);
}

export type SetGatingProgramInstruction<
  TProgram extends string = typeof TOKEN_ACL_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountMintConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountMintConfig extends string
        ? WritableAccount<TAccountMintConfig>
        : TAccountMintConfig,
      ...TRemainingAccounts,
    ]
  >;

export type SetGatingProgramInstructionData = {
  discriminator: number;
  newGatingProgram: Address;
};

export type SetGatingProgramInstructionDataArgs = { newGatingProgram: Address };

export function getSetGatingProgramInstructionDataEncoder(): FixedSizeEncoder<SetGatingProgramInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["newGatingProgram", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_GATING_PROGRAM_DISCRIMINATOR }),
  );
}

export function getSetGatingProgramInstructionDataDecoder(): FixedSizeDecoder<SetGatingProgramInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["newGatingProgram", getAddressDecoder()],
  ]);
}

export function getSetGatingProgramInstructionDataCodec(): FixedSizeCodec<
  SetGatingProgramInstructionDataArgs,
  SetGatingProgramInstructionData
> {
  return combineCodec(
    getSetGatingProgramInstructionDataEncoder(),
    getSetGatingProgramInstructionDataDecoder(),
  );
}

export type SetGatingProgramInput<
  TAccountAuthority extends string = string,
  TAccountMintConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  mintConfig: Address<TAccountMintConfig>;
  newGatingProgram: SetGatingProgramInstructionDataArgs["newGatingProgram"];
};

export function getSetGatingProgramInstruction<
  TAccountAuthority extends string,
  TAccountMintConfig extends string,
  TProgramAddress extends Address = typeof TOKEN_ACL_PROGRAM_ADDRESS,
>(
  input: SetGatingProgramInput<TAccountAuthority, TAccountMintConfig>,
  config?: { programAddress?: TProgramAddress },
): SetGatingProgramInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountMintConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? TOKEN_ACL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    mintConfig: { value: input.mintConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.mintConfig),
    ],
    data: getSetGatingProgramInstructionDataEncoder().encode(
      args as SetGatingProgramInstructionDataArgs,
    ),
    programAddress,
  } as SetGatingProgramInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountMintConfig
  >);
}

export type ParsedSetGatingProgramInstruction<
  TProgram extends string = typeof TOKEN_ACL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    mintConfig: TAccountMetas[1];
  };
  data: SetGatingProgramInstructionData;
};

export function parseSetGatingProgramInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetGatingProgramInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), mintConfig: getNextAccount() },
    data: getSetGatingProgramInstructionDataDecoder().decode(instruction.data),
  };
}
